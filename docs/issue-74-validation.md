# Issue 74 validation

The opportunity inspector now uses the shared `ApplicationWorkflow` preparation form.

Regression coverage verifies that application URL, CV/resume version, and preparation notes are submitted instead of an empty payload.
